import * as LoginActionsConst from './CompleteProfileActionsConst';

const DEFAULT_STATE = {
  accessToken: null,
};

const CompleteProfileReducers = (state = DEFAULT_STATE, {type, data}) => {
  switch (type) {
    default:
      return state;
  }
};

export default CompleteProfileReducers;
