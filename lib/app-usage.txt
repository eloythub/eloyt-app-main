
    -h | --help
        Show you the help of the script.

    -a | --adb <port>
        Adb Service watch
            default port: "8081"

    -s | --start
        Start Packager

    -t | --test
        Start Test

    -r | --run <android|ios>
        Run React Native App On requested platform android|ios

    -rd | --run-device <android|ios>
        Run React Native App On requested platform android|ios

    -re | --release <android|ios>
        Release React Native App On requested platform android|ios

    -red | --release-device <android|ios>
        Release React Native App On requested platform android|ios

    -st | --standard
        Lunch js Standard

    -stf | --standard-fix
        Lunch js Standard fixer

    -c | --cleanup
        Clean up the npm packages
